{
  "name": "unispark-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd unise-php && npm run build",
    "build:admin": "cd admin && npm run build",
    "build:website": "cd unise-php && npm run build"
  }
}
